services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: cdx-rocks:latest
    container_name: cdx-rocks-app
    ports:
      - "8000:8000"
    volumes:
      - ./all_warc_paths.txt.zst:/app/all_warc_paths.txt.zst
      - ./rocksdb_index:/app/data
    environment:
      - ENV=production
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
